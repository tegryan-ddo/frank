# Prod environment manifest
# See: https://aws.github.io/copilot-cli/docs/manifest/environment/

name: prod
type: Environment

# Use default VPC configuration
# Copilot will create a new VPC with public and private subnets

http:
  public:
    certificates:
      # Add your ACM certificate ARN here for HTTPS
      # - arn:aws:acm:us-east-1:123456789:certificate/abc-123
