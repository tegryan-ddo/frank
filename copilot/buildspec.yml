# CodeBuild buildspec for Frank pipeline
# This file is used by AWS CodePipeline to build and deploy the service

version: 0.2

env:
  variables:
    DOCKER_BUILDKIT: "1"
    COPILOT_ENVIRONMENT_NAME: "dev"

phases:
  install:
    commands:
      - echo "Installing AWS Copilot CLI..."
      - curl -fsSL -o /tmp/copilot https://github.com/aws/copilot-cli/releases/latest/download/copilot-linux
      - chmod +x /tmp/copilot
      - mv /tmp/copilot /usr/local/bin/copilot
      - copilot --version
      - echo "Starting Docker daemon..."
      - nohup /usr/local/bin/dockerd --host=unix:///var/run/docker.sock --host=tcp://127.0.0.1:2375 --storage-driver=overlay2 &
      - timeout 15 sh -c "until docker info; do echo .; sleep 1; done"

  pre_build:
    commands:
      - echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com
      - echo "Pre-build phase complete"
      - echo "Workspace contents:"
      - ls -la
      - cat copilot/.workspace || echo "No .workspace file"

  build:
    commands:
      - echo "Building and packaging Frank for env=$COPILOT_ENVIRONMENT_NAME..."
      - echo "Build started on $(date)"
      - echo "Docker info:"
      - docker info || echo "Docker not available"
      - echo "Running copilot svc package..."
      - copilot svc package -n frank -e $COPILOT_ENVIRONMENT_NAME --output-dir ./infrastructure --tag ${CODEBUILD_RESOLVED_SOURCE_VERSION:-latest} 2>&1 || echo "Package failed, checking error..."

  post_build:
    commands:
      - echo "Build completed on $(date)"
      - ls -la ./infrastructure/ 2>/dev/null || echo "No infrastructure output"

cache:
  paths:
    - '/root/.docker/**/*'
